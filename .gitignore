# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.env
.venv
env/
venv/

# Conda
.conda/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Personal documents — binary formats and personal markdown sources
profile/*.docx
profile/*.pdf
profile/Jacob Andersen-Lum Resume.md
profile/Jacob Andersen-Lum Cover Letter Template.md

# Scraped job descriptions (generated output)
job_descriptions/*.md

# Cover letters — all formats (generated output)
cover_letters/**/*.md
cover_letters/**/*.docx
cover_letters/**/*.pdf

# Playwright (fallback — not actively used)
playwright-report/
test-results/

# Misc
*.log
.cache/

# Aggregator — generated output, regenerated on every run
jobs.db
dashboard.html
export.xlsx
.aggregate.lock

# Gmail OAuth — per-person, never shared/committed
credentials.json
token.json

# Outlook OAuth — per-person, never shared/committed
token_outlook.json

# Downloaded Google OAuth client secret — never commit
client_secret_*.json
